label:
  de: Bild
  en: Image
extends: fields/files
multiple: false
uploads:
  template: image
image:
  cover: true
query: site.find('media').images.filterBy('template','image').sortBy('uploaded').flip
